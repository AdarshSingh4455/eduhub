# EduHub Backend

This folder holds backend-only configuration and API logic. Keep API keys in `backend/.env`; do not place them in `frontend/script.js`.

Run the backend with:

```powershell
node server.js
```

Available endpoints:

- `GET /api/config-status`
- `POST /api/chat`
